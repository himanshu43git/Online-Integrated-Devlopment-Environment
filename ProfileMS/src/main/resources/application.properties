# --------------------
# App Identity & Server
# --------------------
spring.application.name=ProfileMS
server.port=10002

# --------------------
# DataSource Settings
# --------------------
spring.datasource.url=jdbc:mysql://localhost:3306/oideProfiles?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=himanshu142bhatt@mysql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --------------------
# Hibernate / JPA Settings
# --------------------
spring.jpa.hibernate.ddl-auto=update       
spring.jpa.show-sql=true                   
spring.jpa.properties.hibernate.format_sql=true

# Optional: Log level for SQL output (for cleaner formatting)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
